import React from 'react';
import { render, fireEvent, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import Display from './../Display';

test('proplar olmadan hatasız çalışıyor', async () => { });

test('butona tıklandığında show bileşeni render ediliyor ', () => { });

test('butona tıklandığında sezon optionları datadakilerle aynı şekide render ediliyor', () => { });
